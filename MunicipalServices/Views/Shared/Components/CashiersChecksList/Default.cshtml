@model IQueryable<MunicipalServices.Data.CashiersCheck>

@{
    ViewData["Title"] = "الشيكات";
    int numberIndex = 0;
}

@(Html
    .Grid(Model)
    .Build(columns =>
    {
        columns.Add(model => model.CheckNumber).Titled("رقم الشيك");
        columns.Add(model => model.AccountNumber).Titled("رقم الحساب");
        columns.Add(model => model.CodeBank).Titled("رمز البنك");
        columns.Add(model => model.BankName).Titled("إسم البنك");
        columns.Add(model => model.CodeBranch).Titled("رمز الفرع");
        columns.Add(model => model.BranchName).Titled("إسم الفرع");
        columns.Add(model => model.DateOfTheWorthy).Titled("يستحق في");
        columns.Add(model => model.AmountOfMoney).Titled("المبلغ");
        columns.Add(model => model.User.FullName).Titled("تم بواسطة");
        columns.Add(model => $"<div class=\"dropdown\">" +
        $"<button class=\"table-dropdow-btn btn\" type=\"button\" data-toggle=\"dropdown\" aria-haspopup=\"true\" aria-expanded=\"false\">...</button>" +
        $"<div class=\"dropdown-menu dropdown-menu-right\" aria-labelledby=\"dropdownmenubutton\">" +
        $"<a class=\"dropdown-item\" href=\"CashiersChecks/edit/{model.ID}\">تعديل</a>" +
        $"<a class=\"dropdown-item\" href=\"CashiersChecks/details/{model.ID}\">تفاصيل</a>" +
        $"<a class=\"dropdown-item\" href=\"CashiersChecks/delete/{model.ID}\">حذف</a>" +
        $"</div></div>").Encoded(false);
    })
    .Using(GridFilterMode.Row)
    .Empty("No data found")
    .Filterable()
    .Sortable()
    .Pageable()
)

@*<div class="table-responsive">
    <table class="roots-table table borderless" cellspacing="0">
        <thead>
            <tr class="table-row-thead">
                <th scope="col">الرقم</th>
                <th scope="col">@Html.DisplayNameFor(model => model.CheckNumber)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.AccountNumber)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.CodeBank)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.BankName)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.CodeBranch)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.DateOfTheWorthy)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.AmountOfMoney)</th>
                <th scope="col">@Html.DisplayNameFor(model => model.User)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="table-row-item">
                    <td class="table-bold-text">@(numberIndex = numberIndex + 1)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.CheckNumber)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.AccountNumber)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.CodeBank)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.BankName)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.CodeBranch)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.DateOfTheWorthy)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.AmountOfMoney)</td>
                    <td class="table-bold-text">@Html.DisplayFor(modelItem => item.User.FullName)</td>
                    <td class="table-text table-product-more-btn">
                        <div class="dropdown">
                            <button class="table-dropdow-btn btn" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">...</button>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownmenubutton">
                                @if (item.CatchReceiptID != null)
                                {
                                    <a class="dropdown-item" asp-action="edit" asp-controller="CashiersChecks" asp-route-id="@item.ID" asp-route-CheckReceiptID="@item.CatchReceiptID" asp-route-IsCatchReceipts="true">Edit</a>
                                }
                                else if (item.ReceiptID != null)
                                {
                                    <a class="dropdown-item" asp-action="edit" asp-controller="CashiersChecks" asp-route-id="@item.ID" asp-route-CheckReceiptID="@item.ReceiptID" asp-route-IsCatchReceipts="false">Edit</a>
                                }
                                <a class="dropdown-item" asp-action="details" asp-controller="CashiersChecks" asp-route-id="@item.ID">Details</a>
                                <a class="dropdown-item" asp-action="delete" asp-controller="CashiersChecks" asp-route-id="@item.ID">Delete</a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>*@